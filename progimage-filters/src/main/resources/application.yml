micronaut:
  application:
    name: progimage-filters
  server:
    port: 10004
    multipart:
      max-file-size: 10MB
endpoints:
  info:
    enabled: true
    sensitive: false
  health:
    enabled: true
    sensitive: false
    details-visible: anonymous
