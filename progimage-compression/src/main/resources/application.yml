micronaut:
  application:
    name: progimage-compression
  server:
    port: 10002
    multipart:
      max-file-size: 10MB
endpoints:
  info:
    enabled: true
    sensitive: false
  health:
    enabled: true
    sensitive: false
    details-visible: anonymous
